//why React ? 
//make easy to manage & build complex frontend .
//React is a library (cool dude - strict rules nhi hote)
//SPA = Single Page Application (page not reload)
// Cons = no  SEO ,  browser render of js slow , no routing 

//for web ==> react + react-dom 
//for mobile apps ==> react + react-native

//npx = Node Package Executor 
//npx → old method of “run this tool temporarily”
// npm create → new method of “run this tool temporarily”

// CRA(Create-React-app) / Vite hume ek already set environment dete hai jisme hum react use kar sake 
//CRA (old) ==> npx create-react-app my-app
//Vite(modern) ==> npm create vite@latest my-app

React ka Dom = virtual DOM
<> </> ==> isko react me fragment bolte hai hum app.jsx me bas ek hi element ko return kar sakte 
{username} => fragment ke andar aise variables define karte hai inko evaluated expression bolte hai .

1. Components (joh functions) banate hai voh capitalized rakho (in both)
2. vite me specifically usse .tsx se banao react me .js bhi chalta hai 

step 0 : humne khud ka ek custom reactElement(object) banaya customRender fnction bhi banaya do methods se aur usse root me append bhi kia ;)  
step 1 : humne check kia ki normal html joh hum return kar rhe hai voh object me convert ho rha react me aur hir render ho rha 
step 2 : phir humne socha direct object hi dede toh ek step bachega , but hume react ke object ke name conventions nhi pata 
step 3 : hume React.createElement() ke baare me pata chala 
    1st arg : element type
    2nd arg : attributes
    3rd arg : children (text joh inject karna hai)
    4th arg : evaluated expression

setCount = “React, update your memory (empty box), please.”
After setCount React does a re-render of your component.
useState always returns whatever value React last stored.
Mutating counter++ does nothing unless you send that new value into setCount.